/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["cdn.sanity.io","images.clerk.dev","www.gravatar.com", "flowbite.s3.amazonaws.com" ]
     }
}

 module.exports = nextConfig
